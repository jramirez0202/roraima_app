<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-800">Editar Zona: <%= @zone.name %></h1>
    <p class="text-gray-600 mt-2">Modifica la información y comunas asignadas a esta zona</p>
  </div>

  <%= render "form", zone: @zone %>
</div>
