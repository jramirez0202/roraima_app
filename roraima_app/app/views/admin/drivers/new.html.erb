<div class="px-4 sm:px-6 lg:px-8 py-8">
  <%= link_to admin_drivers_path, class: "text-green-600 hover:text-green-900 mb-4 inline-flex items-center" do %>
    ← Volver a la lista
  <% end %>

  <div class="md:grid md:grid-cols-3 md:gap-6">
    <div class="md:col-span-1">
      <h3 class="text-lg font-medium text-gray-900">Nuevo Conductor</h3>
      <p class="mt-1 text-sm text-gray-600">
        Crea un nuevo conductor con información vehicular y zona asignada.
      </p>
      <p class="mt-2 text-sm text-gray-600">
        Los campos marcados con * son obligatorios.
      </p>
    </div>

    <div class="md:col-span-2 mt-5 md:mt-0">
      <%= render "form", driver: @driver %>
    </div>
  </div>
</div>
