<div class="px-4 sm:px-6 lg:px-8">
  <div class="sm:flex sm:items-center sm:justify-between">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-semibold text-gray-900">Configuración del Sistema</h1>
      <p class="mt-2 text-sm text-gray-700">
        Ajusta las opciones globales del sistema de gestión de rutas y conductores.
      </p>
    </div>
  </div>

  <%= form_with model: @setting, url: admin_settings_path, method: :patch, local: true, class: "mt-8" do |form| %>
    <div class="space-y-6">
      <!-- Sección: Gestión de Conductores -->
      <div class="bg-white shadow rounded-lg">
        <div class="px-4 py-5 sm:p-6">
          <h3 class="text-lg font-medium leading-6 text-gray-900 mb-4">
            Gestión de Conductores
          </h3>

          <!-- Toggle: Requerir Autorización de Conductores -->
          <div class="flex items-start">
            <div class="flex h-6 items-center">
              <%= form.check_box :require_driver_authorization,
                  id: 'require_driver_authorization',
                  class: "h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-600" %>
            </div>
            <div class="ml-3 text-sm leading-6">
              <%= form.label :require_driver_authorization, class: "font-medium text-gray-900" do %>
                Requerir autorización para iniciar rutas
              <% end %>
              <p class="text-gray-500 mt-1">
                Cuando está <strong>activado</strong>, los conductores deben ser autorizados explícitamente por un administrador antes de poder iniciar rutas.
                La columna "Autorizado" se mostrará en la tabla de conductores.
              </p>
              <p class="text-gray-500 mt-2">
                Cuando está <strong>desactivado</strong>, cualquier conductor activo puede iniciar rutas sin autorización previa.
                La columna "Autorizado" se ocultará de la tabla.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Sección de Información -->
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a.75.75 0 000 1.5h.253a.25.25 0 01.244.304l-.459 2.066A1.75 1.75 0 0010.747 15H11a.75.75 0 000-1.5h-.253a.25.25 0 01-.244-.304l.459-2.066A1.75 1.75 0 009.253 9H9z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3 flex-1">
            <h3 class="text-sm font-medium text-blue-800">
              Información Importante
            </h3>
            <div class="mt-2 text-sm text-blue-700">
              <ul class="list-disc space-y-1 pl-5">
                <li>Los cambios se aplican inmediatamente después de guardar.</li>
                <li>Esta configuración afecta el comportamiento del sistema de gestión de rutas.</li>
                <li>Puedes cambiar esta configuración en cualquier momento según tus necesidades operativas.</li>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <!-- Botones de Acción -->
      <div class="flex items-center justify-end gap-x-6">
        <%= link_to "Cancelar", admin_root_path, class: "text-sm font-semibold leading-6 text-gray-900" %>
        <%= form.submit "Guardar Configuración",
            class: "rounded-md bg-blue-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-blue-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600" %>
      </div>
    </div>
  <% end %>
</div>
