<% if current_user.customer? && current_user.company_logo.attached? %>
  <%= image_tag current_user.company_logo,
    class: "h-8 sm:h-10 md:h-12 w-auto object-contain max-w-[120px] sm:max-w-[150px] md:max-w-[200px]",
    alt: current_user.company.presence || "Logo de empresa",
    loading: "lazy" %>
<% end %>
