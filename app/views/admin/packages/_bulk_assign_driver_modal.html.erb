<!-- Modal de Asignación Masiva de Driver -->
<div id="bulk-assign-driver-modal"
     class="hidden fixed inset-0 z-50 overflow-y-auto"
     aria-labelledby="modal-title-assign"
     role="dialog"
     aria-modal="true"
     data-bulk-packages-target="assignModal">

  <!-- Backdrop con Blur (mismo que modal de estado) -->
  <div class="fixed inset-0 bg-gray-900 bg-opacity-50 transition-opacity"
       style="backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px);"
       aria-hidden="true"
       data-action="click->bulk-packages#closeAssignModal"></div>

  <!-- Modal Container - Centrado (mismo diseño) -->
  <div class="fixed inset-0 flex items-center justify-center p-4 pointer-events-none">
    <div class="relative transform overflow-hidden rounded-lg bg-white text-left shadow-2xl transition-all w-full sm:w-3/4 md:w-2/3 lg:w-1/2 pointer-events-auto">

      <!-- Header (mismo diseño que modal de estado) -->
      <div class="bg-white px-6 pt-5 pb-4">
        <div class="flex items-start justify-between">
          <div class="flex items-center">
            <div class="mx-auto flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-full bg-green-100 sm:mx-0 sm:h-10 sm:w-10">
              <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
              </svg>
            </div>
            <div class="ml-4">
              <h3 class="text-lg font-semibold leading-6 text-gray-900" id="modal-title-assign">
                Asignar paquetes a driver
              </h3>
              <p class="text-sm text-gray-500 mt-1">
                <span data-bulk-packages-target="assignCount">0</span> paquetes seleccionados
              </p>
            </div>
          </div>
          <button type="button" class="text-gray-400 hover:text-gray-500" data-action="click->bulk-packages#closeAssignModal">
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>
      </div>

      <!-- Body -->
      <div class="bg-white px-6 pb-4">

        <!-- Advertencia de validaciones -->
        <div class="mb-4 p-3 bg-red-50 border-l-4 border-red-400 rounded">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-red-800">
                ⚠️ Solo paquetes en BODEGA
              </h3>
              <div class="mt-1 text-xs text-red-700">
                <ul class="list-disc pl-5 space-y-1">
                  <li>Solo se asignan paquetes en estado <strong>"Bodega"</strong></li>
                  <li>Paquetes en otros estados serán ignorados</li>
                  <li>No se pueden reasignar paquetes con driver ya asignado</li>
                </ul>
              </div>
            </div>
          </div>
        </div>

        <!-- Búsqueda de driver con autocomplete -->
        <div class="mb-4">
          <label class="block text-sm font-medium text-gray-700 mb-3">
            Buscar y seleccionar driver
          </label>
          <div class="relative">
            <input type="text"
                   data-bulk-packages-target="driverSearch"
                   data-action="input->bulk-packages#searchDrivers focus->bulk-packages#clearSearch"
                   placeholder="Escribe 2+ letras para buscar..."
                   class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500">

            <!-- Dropdown de resultados -->
            <div data-bulk-packages-target="driversDropdown"
                 class="hidden absolute z-50 left-0 right-0 mt-1 bg-white shadow-xl rounded border border-gray-300 max-h-80 overflow-y-auto">
              <!-- Resultados se cargan dinámicamente -->
            </div>
          </div>
          <p class="mt-2 text-xs text-gray-500">
            💡 Escribe el nombre del conductor para buscar. Máximo 8 resultados.
          </p>
        </div>

        <!-- Preview de asignación -->
        <div data-bulk-packages-target="assignPreview"
             class="hidden p-4 bg-green-50 border border-green-200 rounded-md">
          <p class="text-sm text-green-900 mb-2">
            <span class="font-semibold">Driver seleccionado:</span>
            <span data-bulk-packages-target="selectedDriverName" class="font-bold"></span>
          </p>
          <p class="text-sm text-green-900">
            <span class="font-semibold">Paquetes actuales:</span>
            <span data-bulk-packages-target="currentAssigned" class="font-bold">0</span>
          </p>
          <p class="text-sm text-green-900">
            <span class="font-semibold">Paquetes a asignar:</span>
            +<span data-bulk-packages-target="newAssigned" class="font-bold">0</span>
          </p>
          <p class="text-sm text-green-900 mt-2 pt-2 border-t border-green-300">
            <span class="font-semibold">Total final:</span>
            <span data-bulk-packages-target="totalAssigned" class="font-bold text-lg">0</span>
            paquetes
          </p>
        </div>
      </div>

      <!-- Footer (mismo diseño que modal de estado) -->
      <div class="bg-gray-50 px-6 py-4 flex flex-col-reverse sm:flex-row-reverse gap-3">
        <button type="button"
                data-bulk-packages-target="assignApplyBtn"
                data-action="click->bulk-packages#confirmAssignDriver"
                disabled
                class="w-full sm:w-auto inline-flex justify-center rounded-md border border-transparent bg-green-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed">
          Confirmar Asignación
        </button>
        <button type="button"
                data-action="click->bulk-packages#closeAssignModal"
                class="w-full sm:w-auto inline-flex justify-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
          Cancelar
        </button>
      </div>

    </div>
  </div>
</div>
